# Connexion Neo4j — valeurs par défaut du conteneur décrit dans le README
NEO4J_URI=bolt://localhost:7689
NEO4J_USER=neo4j
NEO4J_PASSWORD=scigraph123

# Optionnel : active la génération de réponse rédigée (sinon le pipeline
# affiche le contexte structuré sans rédaction). Nécessite aussi `pip install anthropic`.
# ANTHROPIC_API_KEY=sk-...
